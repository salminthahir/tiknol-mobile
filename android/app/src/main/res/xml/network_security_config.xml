<?xml version="1.0" encoding="utf-8"?>
<!--
  PV-4: Block cleartext (HTTP) traffic by default. Production talks to HTTPS
  only. The LAN dev host below is the ONLY permitted cleartext destination so
  local development against a dev server keeps working; remove it for a
  hardened production-only build if desired.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <!-- Development-only cleartext allowance (LAN). -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">192.168.18.52</domain>
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
